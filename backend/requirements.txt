# Python: 3.11.x (repo ../.python-version; backend/pyproject.toml requires-python)
# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3

# Validation
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyGithub>=2.0.0
cryptography>=42.0.0

# Cache / Pub-Sub
redis==5.1.1

# AI (LLM: LLM_PROVIDER=google | groq — app.llm.factory.create_chat_llm)
langchain==0.3.0
langchain-groq==0.2.0
langchain-openai>=0.3.0,<0.4
langchain-google-genai==2.0.0
langgraph>=0.3.0,<0.4.0
langgraph-checkpoint-postgres>=2.0.25,<3
psycopg[binary]>=3.2.0

# Lint (pipeline CI lint step — app.pipeline.step_runner.run_lint)
ruff==0.6.9

# Test (pipeline CI test step — app.pipeline.step_runner runs `python -m pytest`)
pytest==8.3.3

# Git
gitpython==3.1.43

# HTTP client (for LangChain internals and tests)
httpx==0.27.2

whatthepatch==1.0.6  # D-04: parse unified diff → Monaco original/modified strings

# Codebase map (US14) — pins per plan.md; grammar smoke tests: tests/unit/test_tree_sitter_grammars.py (T101)
tree-sitter==0.23.0
tree-sitter-python==0.23.2
tree-sitter-javascript==0.23.0
tree-sitter-typescript==0.23.0
